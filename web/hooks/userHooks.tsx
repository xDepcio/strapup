'use client'

export function useHasUserStarredTemplate() {
    // const { data, status, update } = useSession()
    // const [stars, setStars] = useState<string[]>([])

    // useEffect(() => {
    //     if (status === "authenticated") {
    //         setStars(data.user.stars)
    //     }
    // }, [status])

    // return { stars, setStars }
}
